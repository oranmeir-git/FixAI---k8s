apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: fixai
  labels:
    app.kubernetes.io/name: mysql
    app.kubernetes.io/part-of: fixai
    app.kubernetes.io/component: database
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: mysql
  ports:
    - port: 3306
      targetPort: 3306
      protocol: TCP
