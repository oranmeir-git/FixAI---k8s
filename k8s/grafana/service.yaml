apiVersion: v1
kind: Service
metadata:
  name: grafana
  namespace: fixai
  labels:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/part-of: fixai
    app.kubernetes.io/component: monitoring
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: grafana
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
